<resources xmlns:tools="http://schemas.android.com/tools">

    <!-- Base theme, Day only -->
    <style name="Theme.Digivahan" parent="Theme.Material3.Light.NoActionBar">
        <!-- Core colors -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorOnPrimary">@color/white</item>

        <item name="colorSecondary">@color/widgetColor</item>
        <item name="colorOnSecondary">@color/white</item>

        <!-- Status & Navigation bars -->
        <item name="android:statusBarColor">@color/white</item>
        <item name="android:navigationBarColor">@color/white</item>
        <item name="android:windowLightStatusBar">true</item> <!-- dark icons -->

        <!-- Action Bar (if using Toolbar, apply manually) -->
        <item name="actionBarTheme">@style/ActionBarTheme.Digivahan</item>

        <item name="android:windowBackground">@color/white</item>
    </style>

    <style name="RemoveAppSplashTheme" parent="@style/Theme.Digivahan">
        <item name="android:windowIsTranslucent">true</item>
        <item name="android:navigationBarColor">@android:color/transparent</item>
        <item name="android:windowLightNavigationBar" tools:targetApi="o_mr1">false</item>
        <item name="android:enforceNavigationBarContrast" tools:targetApi="q">false</item>
    </style>

    <!-- Action Bar style -->
    <style name="ActionBarTheme.Digivahan" parent="ThemeOverlay.Material3.ActionBar">
        <item name="android:background">@color/actionBarColor</item>
        <item name="titleTextStyle">@style/ActionBarTitleText</item>
    </style>

    <style name="ActionBarTitleText" parent="TextAppearance.Material3.TitleLarge">
        <item name="android:textColor">@color/white</item>
    </style>

    <style name="BottomSheetDialogTheme" parent="Theme.Design.Light.BottomSheetDialog">
        <item name="bottomSheetStyle">@style/CustomBottomSheetStyle</item>

    </style>

    <style name="CustomBottomSheetStyle" parent="Widget.Design.BottomSheet.Modal">
        <item name="android:background">@drawable/bg_bottom_sheet</item>
        <item name="android:windowSoftInputMode">adjustResize</item>
    </style>

    <style name="OrderRow">
        <item name="android:layout_width">match_parent</item>
        <item name="android:layout_height">wrap_content</item>
        <item name="android:paddingVertical">14dp</item>
        <item name="android:textColor">#000000</item>
        <item name="android:textSize">14sp</item>
        <item name="android:fontFamily">@font/hind</item>
        <item name="android:drawableRight">@drawable/ic_arrow_right</item>
    </style>

    <style name="RoundedImageShape">
        <item name="cornerFamily">rounded</item>
        <item name="cornerSize">20dp</item>
    </style>

    <style name="CircularImageShape">
        <item name="cornerFamily">rounded</item>
        <item name="cornerSize">50%</item>
    </style>


</resources>

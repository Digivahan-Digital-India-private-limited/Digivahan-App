<?xml version="1.0" encoding="utf-8"?>
<resources xmlns:tools="http://schemas.android.com/tools">

    <style name="CustomRatingBar" parent="@style/Widget.AppCompat.RatingBar">
        <item name="android:progressDrawable">@drawable/custom_rating_star</item>
        <item name="android:minHeight">24dp</item>
        <item name="android:maxHeight">24dp</item>
    </style>

    <style name="GenderItemStyle">
        <item name="android:layout_width">wrap_content</item>
        <item name="android:layout_height">40dp</item>
        <item name="android:paddingHorizontal">20dp</item>
        <item name="android:gravity">center</item>
        <item name="android:textColor">@android:color/black</item>
        <item name="android:background">@drawable/bg_card_boarder</item>
        <item name="android:layout_marginHorizontal">5dp</item>
        <item name="android:textSize">14sp</item>
    </style>

    <style name="CustomDialogTheme" parent="ThemeOverlay.MaterialComponents.Dialog">

        <!-- 🔹 Transparent background -->
        <item name="android:windowBackground">@android:color/transparent</item>

        <!-- 🔹 Remove default dialog frame -->
        <item name="android:windowFrame">@null</item>

        <!-- 🔹 Disable default floating window padding -->
        <item name="android:windowIsFloating">true</item>

        <!-- 🔹 No title bar -->
        <item name="android:windowNoTitle">true</item>

        <!-- 🔹 Allow rounded corners without clipping -->
        <item name="android:windowClipToOutline">false</item>

        <!-- 🔹 Dim background behind dialog -->
        <item name="android:backgroundDimEnabled">true</item>
        <item name="android:backgroundDimAmount">0.6</item>

        <!-- 🔹 Smooth animations (optional but professional) -->
        <item name="android:windowAnimationStyle">@style/DialogAnimationFade</item>

    </style>

    <style name="DialogAnimationFade">
        <item name="android:windowEnterAnimation">@android:anim/fade_in</item>
        <item name="android:windowExitAnimation">@android:anim/fade_out</item>
    </style>


    <style name="UCrop.FixInsets" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Important -->
        <item name="android:fitsSystemWindows">true</item>

        <!-- Prevent edge-to-edge -->
        <item name="android:windowDrawsSystemBarBackgrounds">true</item>
        <item name="android:statusBarColor">@android:color/white</item>
        <item name="android:navigationBarColor">@android:color/white</item>

        <!-- UCrop colors -->
<!--        <item name="ucrop_toolbar_widget_color">@android:color/black</item>-->
<!--        <item name="ucrop_statusbar_color">@android:color/white</item>-->
<!--        <item name="ucrop_toolbar_color">@android:color/white</item>-->
    </style>

    <style name="RazorpayCustomTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <item name="android:windowTranslucentNavigation">true</item>
        <item name="android:windowTranslucentStatus">true</item>
        <item name="android:windowDrawsSystemBarBackgrounds">true</item>
        <item name="android:fitsSystemWindows">true</item>
    </style>



</resources>